# Participant environment (Google Colab compatible).
# The stock Colab CPU runtime already satisfies these — no installs should be
# required there. Bounded ranges, no OS-specific wheels.
#
# Tested locally with: Python 3.12.9, torch 2.13.0, numpy 2.5.1.
# Expected compatible range: Python 3.10–3.12, torch >= 2.1.
torch>=2.1,<3
numpy>=1.24,<3
